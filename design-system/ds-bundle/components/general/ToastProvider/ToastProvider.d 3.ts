import * as React from 'react';

/**
 * ToastProvider — from mygoodbooks-ds@0.1.0.
 */
export interface ToastProviderProps {
  children?: React.ReactNode;
}

export declare const ToastProvider: React.ComponentType<ToastProviderProps>;
